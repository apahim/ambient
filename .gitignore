# ACP authentication key — NEVER commit
.ambient.key

# Reports directory is intentionally tracked in git.
# Agent sessions write reports here and autoPush commits them.
# Do NOT add reports/ to .gitignore.
